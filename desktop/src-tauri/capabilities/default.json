{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the MusicIdx desktop app.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
